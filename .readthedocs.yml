version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"

sphinx:
  fail_on_warning: true

python:
  version: "3.9.7"
  install:
    - method: pip
      path: .
    - requirements: ./requirements.txt

formats:
  - pdf
  - epub
